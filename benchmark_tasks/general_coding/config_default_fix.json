{
  "id": "config_default_fix",
  "name": "Configuration default precedence fix",
  "repo_path": ".",
  "instruction": "Fix a configuration default/override bug where explicit user input should win over fallback values. Add regression tests for precedence rules.",
  "category": "config/default behavior fix",
  "tags": [
    "config",
    "defaults",
    "precedence"
  ],
  "difficulty": "medium",
  "expected_edit_scope": "1-2 files",
  "timeout_seconds": 300,
  "validation_checks": [
    {
      "type": "command",
      "command": "python -m pytest -q",
      "expect_exit_code": 0
    }
  ],
  "pack_classification": "general_coding"
}
