A smoke-test script for a local web app is hanging because it mishandles a long-lived server process. Repair the workflow so it starts the service, waits for readiness, probes it, and always cleans up.
