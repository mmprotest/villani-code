This suite is designed to cover gaps left by the easier baseline suites.

Focus areas:
- multi-file precedence and configuration interaction
- stateful cache and header mutation bugs
- localization under noisy decoy files
- terminal workflows with cwd sensitivity, deterministic artifacts, and profile-aware behavior
- hidden checks that punish shallow fixes via nearby regressions
