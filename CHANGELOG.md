# Changelog

## 0.1.0rc1

- Added a lightweight search-hit candidate ledger with truncated-discovery warnings and repo-relative path normalization so repo-wide tasks retain unresolved files surfaced by discovery commands without adding task-specific logic.
