import http from 'node:http';
import type { Context, Model, SimpleStreamOptions } from '@earendil-works/pi-ai';
export function zeroUsage(){return {prompt_tokens:0,completion_tokens:0,total_tokens:0};}
function sanitize(e:unknown){return String((e as Error)?.message||e).replace(/Bearer\s+\S+/ig,'Bearer [redacted]').replace(/sk-[A-Za-z0-9_-]+/g,'[redacted]').replace(/(OPENAI_API_KEY|ANTHROPIC_API_KEY|VILLANI_API_KEY)=[^\s]+/ig,'$1=[redacted]');}
export type PiModel = Model<any> & { api?: { streamSimple?: (model:Model<any>, context:Context, options?:SimpleStreamOptions)=>AsyncIterable<any> } };
export function resolvePiModel(ctx:{model?:PiModel,modelRegistry?:any}):PiModel{const m=ctx.model; if(!m) throw new Error('Villani requires an active Pi model; select a Pi model before running /villani.'); if(typeof m.api?.streamSimple!=='function') throw new Error('Active Pi model does not expose streamSimple; cannot proxy Pi model context to Villani.'); return m;}
function toPiMessages(messages:any[]){return (messages||[]).map(m=>{if(m.role==='tool')return {role:'tool',toolCallId:m.tool_call_id,content:m.content}; return m;});}
async function collect(model:PiModel, context:Context, signal?:AbortSignal):Promise<{text:string,tool_calls:any[]}>{let text=''; const tool_calls:any[]=[]; for await (const event of model.api!.streamSimple!(model,context,{signal})){if(signal?.aborted) throw new Error('aborted'); const any=event as any; text+=any.textDelta??any.delta??any.text??any.content??''; const calls=any.toolCalls??any.tool_calls??(any.toolCall?[any.toolCall]:undefined); if(Array.isArray(calls)) for(const c of calls) tool_calls.push({id:c.id, type:'function', function:{name:c.name??c.function?.name, arguments: typeof c.arguments==='string'?c.arguments:JSON.stringify(c.arguments??c.function?.arguments??{})}});} return {text,tool_calls};}
export async function startModelProxy(model:PiModel, signal?:AbortSignal){const server=http.createServer(async(req,res)=>{if(req.method!=='POST'||(req.url||'').split('?')[0]!=='/v1/chat/completions'){res.writeHead(404).end();return;} let body=''; req.on('data',d=>body+=d); req.on('end',async()=>{try{const j=JSON.parse(body||'{}'); const context={messages:toPiMessages(j.messages||[]),tools:j.tools} as unknown as Context; const got=await collect(model,context,signal); const msg:any={role:'assistant',content:got.text}; if(got.tool_calls.length) msg.tool_calls=got.tool_calls; const chunk={id:'pi-villani',object:'chat.completion',created:Math.floor(Date.now()/1000),model:j.model||model.id||'pi-current-model',choices:[{index:0,message:msg,finish_reason:got.tool_calls.length?'tool_calls':'stop'}],usage:zeroUsage()}; if(j.stream){res.writeHead(200,{'content-type':'text/event-stream'}); res.write(`data: ${JSON.stringify({...chunk,object:'chat.completion.chunk'})}\n\n`); res.end('data: [DONE]\n\n');} else {res.writeHead(200,{'content-type':'application/json'}); res.end(JSON.stringify(chunk));}}catch(e){res.writeHead(500,{'content-type':'application/json'}); res.end(JSON.stringify({error:{message:sanitize(e)}}));}});}); await new Promise<void>(r=>server.listen(0,'127.0.0.1',r)); signal?.addEventListener('abort',()=>server.close()); const addr=server.address(); if(!addr||typeof addr==='string')throw new Error('Proxy bind failed'); return {url:`http://127.0.0.1:${addr.port}`,close:()=>server.close(),address:addr};}
